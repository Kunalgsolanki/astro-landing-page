---
import { Picture } from "@astrojs/image/components";
import moonImage from "~/assets/logo.png";

const widths = [450, 800, 1200];
const sizes = "50vw";
---

<Picture
  class="h-full w-full object-cover"
  src={moonImage}
  widths={widths}
  sizes={sizes}
  alt="The ridged surface of the moon"
/>
